server.port=8181



#spring.application.name=discovery-client



eureka.client.serviceUrl.defaultZone=http://localhost:8761/eureka/
# eureka.client.serviceUrl.defaultZone=http://${EUREKA_HOST:localhost}:${EUREKA_PORT:${eureka.server.port}}/eureka/



### Register multiple Instances of a Spring Boot Eureka Client from a single host > working
#server.port=${PORT:0}
#eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${random.value}}
eureka.instance.instance-id=${spring.application.name}:${spring.application.instance_id:${server.port}}



api.key=api.key
